# Web3 学习项目 🧪

以太坊 DApp 学习项目，涵盖以下核心能力：

- 了解以太坊 JSON-RPC 规范与 ethers.js 核心 API
- 生成随机钱包、连接 MetaMask
- ERC20 代币转账
- 链上 Transfer 事件监听
- 集成去中心化交易协议（Uniswap Router）进行代币兑换
- 使用 Hardhat 部署与测试智能合约
- NFT 功能（ERC721 合约示例）

## 项目结构

```
.
├── contracts/              # Solidity 智能合约
│   ├── MyNFT.sol
│   └── MyToken.sol
├── frontend/               # React + Vite 前端 DApp
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── styles.css
├── scripts/                # Hardhat 脚本
│   └── deploy.ts
├── src/                    # 复用的 Web3 工具函数
│   ├── dex.ts
│   ├── erc20.ts
│   ├── events.ts
│   ├── index.ts
│   └── wallet.ts
├── test/
│   └── MyToken.test.ts
├── hardhat.config.ts
├── package.json
└── README.md
```

## 环境准备

1. 安装依赖：

   ```bash
   npm install
   ```

2. Hardhat 编译与测试：

   ```bash
   npm run compile
   npm test
   ```

3. 启动前端 DApp：

   ```bash
   npm run dev
   ```

   打开浏览器访问 `http://localhost:5173`

## 智能合约

- `MyToken.sol`：可铸造的 ERC20 代币，构造函数创建初始供应，并提供 `mint` 接口
- `MyNFT.sol`：基础 ERC721 合约，部署时铸造第一个 NFT，提供 `mint` 功能
- `test/MyToken.test.ts`：验证部署与访问控制逻辑
- `scripts/deploy.ts`：一键部署 Token 与 NFT 的脚本

## 前端功能

前端项目位于 `frontend/`，使用 React + TailwindCSS + Vite 构建，核心能力包括：

- **随机钱包生成**：点击按钮即可生成新的地址 / 私钥 / 助记词（仅用于学习）
- **MetaMask 连接**：一键连接浏览器中的 MetaMask，并显示当前账户
- **ERC20 转账**：输入合约地址、目标地址、数量发起转账
- **链上事件监听**：输入合约地址后监听 `Transfer` 事件，实时输出日志
- **DEX 代币兑换**：输入 Router、路径、数量，支持预估与发起 `swapExactTokensForTokens`

所有交互均基于 `src/` 中的工具函数，实现前后端复用。

## 常用命令

| 命令               | 说明                             |
| ------------------ | -------------------------------- |
| `npm run compile`  | 编译 Solidity 合约               |
| `npm test`         | 运行 Hardhat 测试                 |
| `npm run hardhat`  | 进入 Hardhat CLI                 |
| `npm run dev`      | 启动前端开发服务器                |
| `npm run build`    | 构建前端产物                      |
| `npm run preview`  | 预览构建产物                      |

## 注意事项

- 所有密钥仅用于学习示例，不要在主网或生产环境复用
- `swapTokens` 默认使用 20 分钟 deadline，可根据需求调整
- 监听事件 / DEX 兑换等功能需要正确配置网络与 Router 地址
- 运行前端时，请确保浏览器安装了 MetaMask，并授权相关账号

## 下一步扩展

- 使用 Hardhat 编写更多单元测试（覆盖权限、事件、失败场景）
- 集成 Hardhat 网络（`npx hardhat node`）并在本地调试
- 增加多链支持与网络切换
- 对接后端服务或数据库记录交易历史

